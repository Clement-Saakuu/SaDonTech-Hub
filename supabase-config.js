/**
 * SaDonTech Hub | supabase-config.js
 *
 * Fill these in from your Supabase project:
 *   Project → Settings → API
 *
 * SUPABASE_URL   → "Project URL"
 * SUPABASE_ANON_KEY → "anon / public" key
 *
 * The anon key is NOT a secret — it's designed to be public and shipped in
 * client-side code. Access is controlled by the Row Level Security policies
 * in supabase/schema.sql, not by hiding this key. Never put your
 * "service_role" key here or anywhere in this repo — that one IS secret and
 * must only ever be used server-side (see supabase/functions/verify-payment).
 */
const SUPABASE_CONFIG = {
  url: "https://cgwffvsabwecpuuvmohn.supabase.co",
  anonKey: "sb_publishable_hjPly-376M1WYY_TheGcYQ_2fQUVUxt",
  
};
